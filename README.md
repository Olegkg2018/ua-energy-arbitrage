# UA Energy Arbitrage & Price Forecasting AI 🇺🇦

Програмний комплекс для прогнозування погодинних цін на ринку «на добу наперед» (РДН) в Україні та автоматизованого розрахунку оптимального графіка роботи промислових систем накопичення енергії (BESS / АКБ) з метою максимізації прибутку від арбітражу або мінімізації витрат споживання.

---

## 🌟 Основні можливості / Key Features

- **Прогнозування цін РДН (РЦН):** Гібридне прогнозування на основі моделей машинного навчання **XGBoost Regressor** та нейромережі **MLP (Multi-Layer Perceptron)**. Моделі навчаються на безперервному історичному масиві погодинних цін з 1 січня 2021 року по червень 2026 року.
- **Математичний LP-солвер (PuLP & CBC):** Розрахунок ідеального розкладу заряду та розряду АКБ на основі апарату лінійного програмування. Оптимізатор враховує:
  - КПД циклу заряду та розряду (Charge & Discharge Efficiency).
  - Глибину розряду (Min/Max State of Charge).
  - Швидкість деградації (фізичного зносу) осередків на кожен кВт-год циклу.
  - Повну сітку мережевих тарифів України (передача, розподіл, диспетчеризація та маржа постачальника).
- **Синхронізація у реальному часі:** Автоматичний веб-скрейпінг цін поточного місяця безпосередньо з офіційного сайту ДП «Оператор ринку» (`oree.com.ua`) з кэшуванням запитів та обробкою помилок.
- **Погода та генерація:** Оцінка сонячної та вітрової генерації в системі на основі прогнозів **OpenWeather API** та історичних метеоданих Open-Meteo.
- **Зручний Web-інтерфейс:** Glassmorphism UI дашборд із графіками цін, графіками заряду/розряду/SoC батареї, KPI-метриками, збереженням налаштувань (`localStorage`) та автоматичним підтягуванням реальних погодних факторів для ретроспективного аналізу.

---

## 📂 Структура проєкту / Project Structure

```
├── app.py                   # Flask бекенд API та маршрутизація
├── templates/
│   └── index.html           # Веб-інтерфейс (HTML/CSS/JS + Chart.js)
├── src/
│   ├── data_manager.py      # Скрапінг oree.com.ua, запити до OpenWeather, кэшування
│   ├── model_trainer.py     # Очищення даних, лаги, навчання моделей (XGBoost, MLP)
│   └── optimizer.py         # Математичний лінійний оптимізатор PuLP (CBC)
├── data/                    # База даних (CSV) та збережені .pkl моделі
└── README.md                # Опис проєкту
```

---

## 📈 Метрики моделей / AI Model Performance

Точність прогнозування на тестовому сплиті (2025–2026 рр.) після усунення часових зсувів лагів:
- **MLP Neural Network:** $R^2 = \mathbf{0.7527}$, MAE = 1505 грн/МВт-год.
- **XGBoost Regressor:** $R^2 = \mathbf{0.5973}$, MAE = 1788 грн/МВт-год.

*Модель автоматично дообучається на 100% доступних даних для використання в продакшені.*

---

## 🚀 Як запустити локально / How to Run Locally

### 1. Звичайна локальна інсталяція / Standard Local Installation

#### 1.1. Встановіть залежності / Install Dependencies
Рекомендується використовувати віртуальне оточення:
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

#### 1.2. Запустіть веб-сервер / Start the Server
```bash
python3 app.py
```
Відкрийте у браузері [http://127.0.0.1:5000](http://127.0.0.1:5000).

### 2. Запуск в Docker / Run in Docker
Ви можете зібрати образ та запустити програму в ізольованому контейнері Docker. Щоб прокинути веб-інтерфейс на локальний порт **5002**:

#### 2.1. Зберіть Docker-образ:
```bash
docker build -t ua-energy-arbitrage .
```

#### 2.2. Запустіть контейнер:
```bash
docker run -d -p 5002:5000 --name ua-energy-arbitrage ua-energy-arbitrage
```
Відкрийте у браузері [http://127.0.0.1:5002](http://127.0.0.1:5002).

*Якщо ви бажаєте використовувати власні API-ключі чи налаштування з локального `.env` файлу:*
```bash
docker run -d -p 5002:5000 --env-file .env --name ua-energy-arbitrage ua-energy-arbitrage
```

---

## 📝 Щоденний робочий процес / Daily Workflow

1. **Планування розкладу:** Виберіть дату розрахунку (наприклад, завтрашній день).
2. **Введення тарифів:** Вкажіть діючі мережеві тарифи для вашого підприємства та характеристики АКБ у лівій панелі.
3. **Запуск аналізу:** Натисніть кнопку **"Розрахувати Розклад"**. Система завантажить прогноз погоди, ціни РДН, виконає AI-прогноз і побудує оптимальну стратегію роботи батареї.
4. **Експорт:** Використовуйте погодинний графік заряду/розряду для завантаження в EMS накопичувальної системи.
